<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title><%= pantryOwner.username %>'s Pantry</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <div class="all-apps-body">
    <div class="all-apps-header">
      <h1><%= pantryOwner.username %>'s Vegan Baking Pantry</h1>
    </div>
    <% if (pantry.length === 0) { %>
      <p>This pantry is empty!</p>
    <% } else { %>
      <ul>
        <% pantry.forEach((food) => { %>
          <li>
            <strong><%= food.name %></strong>
            <% if (food.brand) { %> - <%= food.brand %><% } %>
            <% if (food.category) { %> (<%= food.category %>)<% } %>
            <% if (food.purchaseLink) { %>
              <br><a href="<%= food.purchaseLink %>" target="_blank">Where to buy</a>
            <% } %>
          </li>
        <% }) %>
      </ul>
    <% } %>
    <br>
    <a href="/users">← Back to Community</a>
  </div>
</body>
</html>