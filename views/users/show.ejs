<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <title><%= pantryOwner.username %>'s Pantry</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <div class="inner-header"></div>
  
  <div class="all-foods-body">
    <div class="content-box">
      <h1 style="color: white; text-align: center; margin-bottom: 30px;"><%= pantryOwner.username %>'s Vegan Baking Pantry</h1>
      
      <% if (pantry.length === 0) { %>
        <p style="color: white; text-align: center;">This pantry is empty!</p>
      <% } else { %>
        <ul>
          <% pantry.forEach((food) => { %>
            <a href="#">
              <li>
                <strong><%= food.name %></strong>
                <% if (food.brand) { %> - <%= food.brand %><% } %>
                <% if (food.category) { %> (<%= food.category %>)<% } %>
              </li>
            </a>
          <% }) %>
        </ul>
      <% } %>
      
      <div style="text-align: center; margin-top: 20px;">
        <a href="/users" style="color: white; text-decoration: underline;">← Back to Community</a>
      </div>
    </div>
  </div>
</body>
</html>